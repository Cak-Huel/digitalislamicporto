<nav
    x-data="{ mobileMenuOpen: false }"
    class="fixed top-0 w-full z-50 bg-background/90 backdrop-blur-md border-b border-outline-variant/60 shadow-sm transition-all duration-300"
>
    <div
        class="flex justify-between items-center px-6 md:px-12 py-4 max-w-full"
    >
        <div class="font-headline text-2xl font-bold text-primary">
            Islamic Portofolio
        </div>

        <!-- Desktop Menu -->
        <div
            class="hidden md:flex space-x-8 items-center"
            x-data="{ activeSection: 'muqaddimah' }"
        >
            <template
                x-for="item in [
                { id: 'muqaddimah', label: 'Muqaddimah' },
                { id: 'jejak-digital', label: 'Jejak Digital' },
                { id: 'galeri-inovasi', label: 'Galeri Inovasi' },
                { id: 'khidmah', label: 'Khidmah' },
                { id: 'strategi', label: 'Strategi' },
                { id: 'visi', label: 'Visi' }
            ]"
                :key="item.id"
            >
                <a
                    :href="'#' + item.id"
                    @click="activeSection = item.id"
                    :class="activeSection === item.id ? 'text-primary border-b-2 border-primary font-semibold' : 'text-on-surface-variant'"
                    class="font-headline text-lg italic hover:text-primary transition-all duration-300 cursor-pointer active:opacity-70"
                    x-text="item.label"
                >
                </a>
            </template>
        </div>

        <a
            href="https://wa.me/6287841410773"
            target="_blank"
            class="hidden md:block bg-primary hover:bg-surface-tint text-on-primary font-body font-medium px-6 py-2 rounded border border-transparent transition-colors duration-300 cursor-pointer active:opacity-70"
        >
            Hubungi Saya
        </a>

        <!-- Hamburger Icon -->
        <button
            @click="mobileMenuOpen = !mobileMenuOpen"
            class="md:hidden text-primary focus:outline-none"
        >
            <span
                class="material-symbols-outlined text-3xl"
                x-text="mobileMenuOpen ? 'close' : 'menu'"></span>
        </button>
    </div>

    <!-- Mobile Menu -->
    <div
        x-show="mobileMenuOpen"
        x-transition
        class="md:hidden bg-surface border-b border-outline-variant/60 shadow-sm absolute w-full"
        style="display: none;"
    >
        <div class="flex flex-col px-6 py-4 space-y-4">
            <a
                @click="mobileMenuOpen = false"
                href="#muqaddimah"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Muqaddimah</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="#jejak-digital"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Jejak Digital</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="#galeri-inovasi"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Galeri Inovasi</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="#khidmah"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Khidmah</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="#strategi"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Strategi</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="#visi"
                class="font-headline text-lg italic text-on-surface hover:text-primary"
                >Visi</a
            >
            <a
                @click="mobileMenuOpen = false"
                href="https://wa.me/6287841410773"
                target="_blank"
                class="bg-primary text-on-primary font-body font-medium px-4 py-2 rounded text-center"
                >Hubungi Saya</a
            >
        </div>
    </div>
</nav>
